# 프로젝트 정보

- 프로젝트 이름: 좌표픽셀
- 한 줄 목표: 슬라이더를 조작해 3목을 완성하는 '좌표픽셀' 보드게임 (현재 단계: AI 봇 대전 프로토타입)
- 사용 대상: 학생(참가자), 교사(관리자)
- 실행 환경: 교실 PC(교사), 개별 태블릿(학생)
- 프로젝트 유형: 다중화면·연동형 (게임·인터랙티브 요소 포함)
- 형태: B 빌드 도구형
- 기술 스택과 버전: Vite + React (추후 Firebase 연동 예정)
- 인터넷 연결 필요 여부: 필수 (프로토타입 단계는 로컬 동작)
- 개인정보 취급 여부: 없음
- 검증 방법: `npm run dev` 실행 후 브라우저에서 AI 봇 대전 정상 동작 확인
- 시작일: 2026-09-04

## 이 프로젝트만의 규칙
(GEMINI.md의 일반 규칙보다 우선한다)
- 슬라이더 2개 중 단 1개만 움직여 교차점에 돌을 두는 규칙을 정확히 구현한다.
- 빈 칸이 아닌 곳으로는 슬라이더를 이동시킬 수 없도록 제약을 둔다.
- 현재는 프로토타입 단계이므로, 방 생성/참가 기능의 UI만 만들고 실제 로직은 로컬 상태와 AI 봇 대전으로 동작하게 한다.
- 추후 교사 관전 및 실시간 연동이 가능하도록, 게임 상태(보드판 배열, 슬라이더 위치, 턴)를 하나의 상태 객체로 분리하여 관리한다.

## 동작 확인 목록
- 방 생성 및 참가 UI가 나타나는가
- 게임 화면에서 가로 또는 세로 슬라이더 중 하나만 조작 가능한가
- 이미 돌이 놓인 교차점으로는 슬라이더 이동이 막히는가
- 교차점에 돌을 두면 턴이 넘어가고 AI 봇이 자동으로 돌을 두는가
- 3개가 연속되면(가로/세로/대각선) 승리 판정이 정상적으로 뜨는가

> 여기 적힌 기술 스택은 확정 사항이다. 사용자 승인 없이 변경하거나 다른 라이브러리를 추가하지 않는다.
